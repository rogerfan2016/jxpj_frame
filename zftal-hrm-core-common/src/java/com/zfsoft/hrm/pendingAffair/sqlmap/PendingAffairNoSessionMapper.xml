<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zfsoft.hrm.pendingAffair.dao.IPendingAffairNoSessionDao">
    <!-- 取得部门名 -->
    <select id="getDwmByUserId" parameterType="string" resultType="string">
        select o.dwm from overall o where o.gh = #{yhm}
    </select>
</mapper>